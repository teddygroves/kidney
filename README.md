# Kidney analysis

To reproduce the analysis:

1. install [uv](https://docs.astral.sh/uv/getting-started/installation/)
2. `sh run_analysis.sh`

This will perform data preparation (script `script/prepare_dat.py`), MCMC (`scripts/run_mcmc.py`) and plotting (`notebooks/analyse fits.ipynb`).
